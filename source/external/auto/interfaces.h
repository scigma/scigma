! interfaces to use instead of EXTERNAL for type checking

  INTERFACE
     
     SUBROUTINE FUNI(AC,NDIM,U,UOLD,ICP,PAR,IJAC,F,DFDU,DFDP)
       USE AUTO_TYPES, ONLY: AUTOCONTEXT
       TYPE(AUTOCONTEXT), INTENT(IN), TARGET :: AC
       INTEGER, INTENT(IN) :: ICP(*),NDIM,IJAC
       DOUBLE PRECISION, INTENT(IN) :: UOLD(*)
       DOUBLE PRECISION, INTENT(INOUT) :: U(NDIM),PAR(*)
       DOUBLE PRECISION, INTENT(OUT) :: F(NDIM)
       DOUBLE PRECISION, INTENT(INOUT) :: DFDU(NDIM,NDIM),DFDP(NDIM,*)
     END SUBROUTINE FUNI
     
     SUBROUTINE FUNIX(AC,U,PAR,DFDU,DFDV)
       USE AUTO_TYPES, ONLY: AUTOCONTEXT
       TYPE(AUTOCONTEXT), INTENT(IN), TARGET :: AC
       DOUBLE PRECISION, INTENT(INOUT) :: U(AC%AP%NDM*2+2),PAR(*)
       DOUBLE PRECISION, INTENT(IN) :: DFDU(AC%AP%NDM,AC%AP%NDM)
       DOUBLE PRECISION, INTENT(OUT) :: DFDV(AC%AP%NDM,AC%AP%NDM+1)
     END SUBROUTINE FUNIX
     
     SUBROUTINE BCNI(AC,NDIM,PAR,ICP,NBC,U0,U1,F,IJAC,DBC)
       USE AUTO_TYPES, ONLY: AUTOCONTEXT
       TYPE(AUTOCONTEXT), INTENT(INOUT), TARGET :: AC
       INTEGER, INTENT(IN) :: NDIM,ICP(*),NBC,IJAC
       DOUBLE PRECISION, INTENT(INOUT) :: U0(NDIM),U1(NDIM),PAR(*)
       DOUBLE PRECISION, INTENT(OUT) :: F(NBC)
       DOUBLE PRECISION, INTENT(INOUT) :: DBC(NBC,*)
     END SUBROUTINE BCNI
     
     SUBROUTINE ICNI(AC,NDIM,PAR,ICP,NINT,U,UOLD,UDOT,UPOLD,F,IJ,D)
       USE AUTO_TYPES, ONLY: AUTOCONTEXT
       TYPE(AUTOCONTEXT), INTENT(IN), TARGET :: AC
       INTEGER, INTENT(IN) :: ICP(*),NDIM,NINT,IJ
       DOUBLE PRECISION, INTENT(IN) :: UOLD(NDIM),UDOT(NDIM)
       DOUBLE PRECISION, INTENT(IN) :: UPOLD(NDIM)
       DOUBLE PRECISION, INTENT(INOUT) :: U(NDIM),PAR(*)
       DOUBLE PRECISION, INTENT(OUT) :: F(NINT)
       DOUBLE PRECISION, INTENT(INOUT) :: D(NINT,*)
     END SUBROUTINE ICNI
     
     SUBROUTINE STPNBVI(AC,PAR,ICP,NTSR,NCOLRS,RLD,UPS,UDOPS,TM,ND)
       USE AUTO_TYPES, ONLY: AUTOCONTEXT
       TYPE(AUTOCONTEXT), INTENT(INOUT), TARGET :: AC
       INTEGER, INTENT(IN) :: ICP(*)
       INTEGER, INTENT(INOUT) :: NTSR,NCOLRS
       INTEGER, INTENT(OUT) :: ND
       DOUBLE PRECISION, INTENT(OUT) :: PAR(*),RLD(AC%AP%NFPR)
       DOUBLE PRECISION, INTENT(OUT) :: UPS(AC%AP%NDIM,0:*)
       DOUBLE PRECISION, INTENT(OUT) :: UDOPS(AC%AP%NDIM,0:*),TM(0:*)
     END SUBROUTINE STPNBVI
     
     SUBROUTINE STPNAEI(AC,PAR,ICP,U,UDOT,NODIR)
       USE AUTO_TYPES, ONLY: AUTOCONTEXT
       TYPE(AUTOCONTEXT), INTENT(IN), TARGET :: AC
       INTEGER, INTENT(IN) :: ICP(*)
       INTEGER, INTENT(OUT) :: NODIR
       DOUBLE PRECISION, INTENT(OUT) :: PAR(*),U(*),UDOT(*)
     END SUBROUTINE STPNAEI
     
     DOUBLE PRECISION FUNCTION FNCI(AC,ICP,UPS,NDIM,PAR,ITEST,ATYPE)
       USE AUTO_TYPES, ONLY: AUTOCONTEXT
       TYPE(AUTOCONTEXT), INTENT(INOUT), TARGET :: AC
       INTEGER, INTENT(IN) :: ICP(*),NDIM
       DOUBLE PRECISION, INTENT(IN) :: UPS(*)
       DOUBLE PRECISION, INTENT(INOUT) :: PAR(*)
       INTEGER, INTENT(IN) :: ITEST
       CHARACTER(LEN=*), INTENT(OUT) :: ATYPE
     END FUNCTION FNCI
     
  END INTERFACE
  
